$("#chatboxes").html("<%= escape_javascript(render partial: "chatroom", local: @chatroom ) %>");
$("#post_chatroom_id").val(<%= escape_javascript(@chatroom.id.to_s) %>);
$("#" + "<%= escape_javascript(@chatroom.name) %>").scrollTop($("#" + "<%= escape_javascript(@chatroom.name) %>")[0].scrollHeight);
