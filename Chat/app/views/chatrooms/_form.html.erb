<%= form_for(chatroom, remote: true) do |f| %>
  <% if chatroom.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(chatroom.errors.count, "error") %> prohibited this chatroom from being saved:</h2>

      <ul>
      <% chatroom.errors.full_messages.each do |message| %>
        <li><%= message %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

    <div class="flexbox-center">
    <div>
    <%= f.text_field :name %>
    </div>
    <div class="align-self">
    <%= f.submit class: "btn btn-primary", id: "button-chatroom-new", remote: true %>
    </div>
    </div>

<% end %>
